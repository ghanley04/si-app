{
  "name": "sista",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "SIsta — SI leader companion app (Vite + Firebase)",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^8.2.2"
  },
  "dependencies": {
    "firebase": "^12.18.0"
  }
}
