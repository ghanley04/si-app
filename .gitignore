# OS junk
.DS_Store

# Local secrets — never commit API keys.
# The demo reads its key from the browser (localStorage) or an optional
# window.CLAUDE_API_KEY set by one of these git-ignored files.
.env
.env.*
config.local.js
*.secret.js
